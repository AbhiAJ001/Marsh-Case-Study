# Marsh Case Study — Python Dependencies
# Architecture v2: Flask + OKF + LangChain fallback

# Backend server
flask>=3.0
groq>=1.7
openai>=1.40        # OpenAI-compatible client for OpenRouter, NVIDIA NIM, Mistral

# LLM — Google Gemini
google-generativeai>=0.8

# Knowledge layer — OKF support
pyyaml>=6.0

# RAG fallback — LangChain + FAISS
langchain>=0.3
langchain-community>=0.3
langchain-google-genai>=2.0
faiss-cpu>=1.7

# PDF processing
PyPDF2>=3.0

# PowerPoint generation
python-pptx>=1.0

# Environment management
python-dotenv>=1.0

# Web search for company research
requests>=2.31
